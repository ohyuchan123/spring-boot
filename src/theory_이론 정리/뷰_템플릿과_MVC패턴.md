# 뷰 템플릿과 MVC 패턴
```
spring initializr에서 Mustache라는 기능을 추가하였는데 이 도구가 View Templates를 
만드는 도구이다.

View Templates에는 Controller 와 Model이라는 동료가 존재하며
Controller(logic)는 처리 과정을 담당하고 Model(data)은 data를 관리합니다.

이렇게 화면,처리,데이터 분야를 각 담당자 별로 나누는 기법을 MVC 패턴이라고 한다.

mustache는 html을 다르게 사용하는 기능(doc 탭 사용하기) 
```